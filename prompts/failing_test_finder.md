Parse the failing tests from the supplied log output.  

Format the test name as fully qualified test_module.test_method

Only methods that start with the prefix "test" are valid test methods.  Never return other helper methods like "setUpClass", etc.

