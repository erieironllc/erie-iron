{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.com/iteration-evaluator-output.schema.json",
  "title": "IterationEvaluatorOutput",
  "description": "Structured output from the iteration_evaluator agent.",
  "type": "object",
  "required": [
    "best_iteration_id",
    "iteration_id_to_modify",
    "previous_iteration_count",
    "is_stagnating"
  ],
  "properties": {
    "best_iteration_id": {
      "type": "string",
      "description": "The ID of the best available code iteration to move forward with."
    },
    "iteration_id_to_modify": {
      "description": "The iteration that the planner should build on. Either 'latest' or a specific iteration ID.",
      "anyOf": [
        { "type": "string", "const": "latest" },
        { "type": "string", "minLength": 1 }
      ]
    },
    "previous_iteration_count": {
      "description": "How many previous iterations the planner should load for context. Typically a small integer.",
      "anyOf": [
        { "type": "integer", "minimum": 1 }
      ]
    },
    "rollback_reason": {
      "type": "string",
      "description": "(optional) A detailed justification explaining why this iteration was chosen as the one to modify next."
    }
  },
  "additionalProperties": false
}