{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "FailureModeRouterOutput",
  "type": "object",
  "required": ["classification", "recovery_path", "recovery_path_explanation", "fix_prompt"],
  "properties": {
    "classification": {
      "type": "string",
      "enum": [
        "SYNTAX_ERROR",
        "IMPORT_ERROR",
        "VERSION_MISMATCH",
        "ATTRIBUTE_ERROR",
        "MISSING_DEPENDENCY",
        "CONFIGURATION_ERROR",
        "NETWORK_ERROR",
        "UNKNOWN"
      ]
    },
    "recovery_path": {
      "type": "string",
      "enum": [
        "DIRECT_FIX",
        "ESCALATE_TO_PLANNER",
        "AWS_PROVISIONING_PLANNER",
        "ESCALATE_TO_HUMAN"
      ]
    },
    "recovery_path_explanation": {
      "type": "string",
      "description": "Your reasoning for why you chose the recovery path"
    },
    "fix_prompt": {
      "type": "string",
      "description": "A suggested next step or command to fix the issue"
    },
    "context_files": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Relative paths of code files needed by the quick fix agent"
    }
  },
  "additionalProperties": false
}