{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.com/iteration-evaluator-output.schema.json",
  "title": "IterationEvaluatorOutput",
  "description": "Structured output from the iteration_evaluator agent.",
  "type": "object",
  "required": [
    "goal_achieved",
    "summary",
    "error"
  ],
  "properties": {
    "goal_achieved": {
      "type": "boolean",
      "description": "True if the GOAL was successfully achieved, false otherwise."
    },
    "summary": {
      "type": "string",
      "description": "A multi-sentence high-level summary of the current iteration's behavior, including any overarching interpretations or theories."
    },
    "error": {
      "type": "object",
      "description": "The first critical error that prevented execution or testing from completing. If no such error occurred, this field should be omitted.",
      "required": ["summary", "logs"],
      "properties": {
        "summary": {
          "type": "string",
          "description": "Short title or classification of the issue (e.g., 'throws KeyError')"
        },
        "logs": {
          "type": "string",
          "description": "Log output or test results that support the summary."
        }
      }
    }
  },
  "additionalProperties": false
}